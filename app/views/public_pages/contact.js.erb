$(".modal-body").html("<%= escape_javascript(render partial: '/public_pages/contact', locals: {  }) %>");
$(".navbar-collapse").collapse('hide');
$("#lambertModal").modal("show");

<% if @remove_backdrop %>
  $(".modal-backdrop").hide();
<% end %>
